void SUB_DELSELECT(){
qbs *tqbs;
ptrszint tmp_long;
int32 tmp_fileno;
uint32 qbs_tmp_base=qbs_tmp_list_nexti;
uint8 *tmp_mem_static_pointer=mem_static_pointer;
uint32 tmp_cmem_sp=cmem_sp;
#include "data374.txt"
mem_lock *sf_mem_lock;
new_mem_lock();
sf_mem_lock=mem_lock_tmp;
sf_mem_lock->type=3;
if (is_error_pending()) goto exit_subfunc;
do{
*_SUB_DELSELECT_LONG_SY1=*__LONG_IDESELECTY1;
if(!qbevent)break;evnt(25528,20535,"ide_methods.bas");}while(r);
do{
*_SUB_DELSELECT_LONG_SY2=*__LONG_IDECY;
if(!qbevent)break;evnt(25528,20536,"ide_methods.bas");}while(r);
S_62319:;
if ((-(*_SUB_DELSELECT_LONG_SY1>*_SUB_DELSELECT_LONG_SY2))||is_error_pending()){
if(qbevent){evnt(25528,20537,"ide_methods.bas");if(r)goto S_62319;}
do{
swap_32(&*_SUB_DELSELECT_LONG_SY1,&*_SUB_DELSELECT_LONG_SY2);
if(!qbevent)break;evnt(25528,20537,"ide_methods.bas");}while(r);
}
do{
*_SUB_DELSELECT_LONG_SX1=*__LONG_IDESELECTX1;
if(!qbevent)break;evnt(25528,20538,"ide_methods.bas");}while(r);
do{
*_SUB_DELSELECT_LONG_SX2=*__LONG_IDECX;
if(!qbevent)break;evnt(25528,20539,"ide_methods.bas");}while(r);
S_62324:;
if ((-(*_SUB_DELSELECT_LONG_SX1>*_SUB_DELSELECT_LONG_SX2))||is_error_pending()){
if(qbevent){evnt(25528,20540,"ide_methods.bas");if(r)goto S_62324;}
do{
swap_32(&*_SUB_DELSELECT_LONG_SX1,&*_SUB_DELSELECT_LONG_SX2);
if(!qbevent)break;evnt(25528,20540,"ide_methods.bas");}while(r);
}
do{
*_SUB_DELSELECT_LONG_NOLASTLINEDEL= 0 ;
if(!qbevent)break;evnt(25528,20541,"ide_methods.bas");}while(r);
S_62328:;
if (((-(*_SUB_DELSELECT_LONG_SY1!=*_SUB_DELSELECT_LONG_SY2))&(-(*__LONG_IDECX== 1 ))&(-(*__LONG_IDECY>*_SUB_DELSELECT_LONG_SY1)))||is_error_pending()){
if(qbevent){evnt(25528,20542,"ide_methods.bas");if(r)goto S_62328;}
do{
*_SUB_DELSELECT_LONG_SY2=*_SUB_DELSELECT_LONG_SY2- 1 ;
if(!qbevent)break;evnt(25528,20542,"ide_methods.bas");}while(r);
do{
*_SUB_DELSELECT_LONG_NOLASTLINEDEL= 1 ;
if(!qbevent)break;evnt(25528,20542,"ide_methods.bas");}while(r);
}
S_62332:;
fornext_value7021=*_SUB_DELSELECT_LONG_SY2;
fornext_finalvalue7021=*_SUB_DELSELECT_LONG_SY1;
fornext_step7021= -1 ;
if (fornext_step7021<0) fornext_step_negative7021=1; else fornext_step_negative7021=0;
if (is_error_pending()) goto fornext_error7021;
goto fornext_entrylabel7021;
while(1){
fornext_value7021=fornext_step7021+(*_SUB_DELSELECT_LONG_Y);
fornext_entrylabel7021:
*_SUB_DELSELECT_LONG_Y=fornext_value7021;
if (fornext_step_negative7021){
if (fornext_value7021<fornext_finalvalue7021) break;
}else{
if (fornext_value7021>fornext_finalvalue7021) break;
}
fornext_error7021:;
if(qbevent){evnt(25528,20545,"ide_methods.bas");if(r)goto S_62332;}
S_62333:;
if (((-(*_SUB_DELSELECT_LONG_SY1==*_SUB_DELSELECT_LONG_SY2))&(-(*_SUB_DELSELECT_LONG_NOLASTLINEDEL== 0 )))||is_error_pending()){
if(qbevent){evnt(25528,20546,"ide_methods.bas");if(r)goto S_62333;}
do{
qbs_set(_SUB_DELSELECT_STRING_A,FUNC_IDEGETLINE(_SUB_DELSELECT_LONG_Y));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(25528,20547,"ide_methods.bas");}while(r);
do{
qbs_set(_SUB_DELSELECT_STRING_A2,qbs_new_txt_len("",0));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(25528,20548,"ide_methods.bas");}while(r);
S_62336:;
if ((qbs_cleanup(qbs_tmp_base,-(*_SUB_DELSELECT_LONG_SX1<=_SUB_DELSELECT_STRING_A->len)))||is_error_pending()){
if(qbevent){evnt(25528,20549,"ide_methods.bas");if(r)goto S_62336;}
do{
qbs_set(_SUB_DELSELECT_STRING_A2,qbs_left(_SUB_DELSELECT_STRING_A,*_SUB_DELSELECT_LONG_SX1- 1 ));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(25528,20549,"ide_methods.bas");}while(r);
}else{
do{
qbs_set(_SUB_DELSELECT_STRING_A2,_SUB_DELSELECT_STRING_A);
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(25528,20549,"ide_methods.bas");}while(r);
}
S_62341:;
if ((qbs_cleanup(qbs_tmp_base,-(*_SUB_DELSELECT_LONG_SX2<=_SUB_DELSELECT_STRING_A->len)))||is_error_pending()){
if(qbevent){evnt(25528,20550,"ide_methods.bas");if(r)goto S_62341;}
do{
qbs_set(_SUB_DELSELECT_STRING_A2,qbs_add(_SUB_DELSELECT_STRING_A2,qbs_right(_SUB_DELSELECT_STRING_A,_SUB_DELSELECT_STRING_A->len-*_SUB_DELSELECT_LONG_SX2+ 1 )));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(25528,20550,"ide_methods.bas");}while(r);
}
do{
SUB_IDESETLINE(_SUB_DELSELECT_LONG_Y,_SUB_DELSELECT_STRING_A2);
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(25528,20551,"ide_methods.bas");}while(r);
}else{
S_62346:;
if (((-(*__LONG_IDEN== 1 ))&(-(*_SUB_DELSELECT_LONG_Y== 1 )))||is_error_pending()){
if(qbevent){evnt(25528,20553,"ide_methods.bas");if(r)goto S_62346;}
do{
SUB_IDESETLINE(_SUB_DELSELECT_LONG_Y,qbs_new_txt_len("",0));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(25528,20553,"ide_methods.bas");}while(r);
}else{
do{
SUB_IDEDELLINE(_SUB_DELSELECT_LONG_Y);
if(!qbevent)break;evnt(25528,20553,"ide_methods.bas");}while(r);
}
}
fornext_continue_7020:;
}
fornext_exit_7020:;
do{
*__LONG_IDECX=*_SUB_DELSELECT_LONG_SX1;
if(!qbevent)break;evnt(25528,20557,"ide_methods.bas");}while(r);
S_62354:;
if (((-(*_SUB_DELSELECT_LONG_SY1!=*_SUB_DELSELECT_LONG_SY2))|(-(*_SUB_DELSELECT_LONG_NOLASTLINEDEL== 1 )))||is_error_pending()){
if(qbevent){evnt(25528,20557,"ide_methods.bas");if(r)goto S_62354;}
do{
*__LONG_IDECX= 1 ;
if(!qbevent)break;evnt(25528,20557,"ide_methods.bas");}while(r);
}
do{
*__LONG_IDECY=*_SUB_DELSELECT_LONG_SY1;
if(!qbevent)break;evnt(25528,20558,"ide_methods.bas");}while(r);
do{
*__LONG_IDESELECT= 0 ;
if(!qbevent)break;evnt(25528,20559,"ide_methods.bas");}while(r);
exit_subfunc:;
free_mem_lock(sf_mem_lock);
#include "free374.txt"
if ((tmp_mem_static_pointer>=mem_static)&&(tmp_mem_static_pointer<=mem_static_limit)) mem_static_pointer=tmp_mem_static_pointer; else mem_static_pointer=mem_static;
cmem_sp=tmp_cmem_sp;
}
