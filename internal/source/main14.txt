void SUB_UDTAPPLYFIELDMODE(qbs*_SUB_UDTAPPLYFIELDMODE_STRING_TOKEN,int32*_SUB_UDTAPPLYFIELDMODE_LONG_FIELDMODE){
qbs *tqbs;
ptrszint tmp_long;
int32 tmp_fileno;
uint32 qbs_tmp_base=qbs_tmp_list_nexti;
uint8 *tmp_mem_static_pointer=mem_static_pointer;
uint32 tmp_cmem_sp=cmem_sp;
#include "data14.txt"
mem_lock *sf_mem_lock;
new_mem_lock();
sf_mem_lock=mem_lock_tmp;
sf_mem_lock->type=3;
if (is_error_pending()) goto exit_subfunc;
error_track_line(15633,0,NULL);
do{
if(!qbevent)break;evnt(15633);}while(r);
error_track_line(15635,0,NULL);
do{
*_SUB_UDTAPPLYFIELDMODE_LONG_PARSED_MODE=FUNC_UDTFIELDMODETOKEN(_SUB_UDTAPPLYFIELDMODE_STRING_TOKEN);
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15635);}while(r);
error_track_line(15636,0,NULL);
S_18143:;
if ((-(*_SUB_UDTAPPLYFIELDMODE_LONG_PARSED_MODE== 0 ))||is_error_pending()){
if(qbevent){evnt(15636);if(r)goto S_18143;}
error_track_line(15636,0,NULL);
do{
goto exit_subfunc;
if(!qbevent)break;evnt(15636);}while(r);
error_track_line(15636,0,NULL);
}
error_track_line(15637,0,NULL);
S_18146:;
if ((-(((int8*)(__ARRAY_BYTE_UNSTABLEFLAGS[0]))[array_check((*__LONG_UNSTABLE_TYPEFIELDS)-__ARRAY_BYTE_UNSTABLEFLAGS[4],__ARRAY_BYTE_UNSTABLEFLAGS[5])]== 0 ))||is_error_pending()){
if(qbevent){evnt(15637);if(r)goto S_18146;}
error_track_line(15638,0,NULL);
do{
SUB_GIVE_ERROR(qbs_new_txt_len("_Static/_Dynamic require $Unstable:TypeFields",45));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15638);}while(r);
error_track_line(15639,0,NULL);
do{
goto exit_subfunc;
if(!qbevent)break;evnt(15639);}while(r);
error_track_line(15640,0,NULL);
}
error_track_line(15641,0,NULL);
S_18150:;
if ((-(*_SUB_UDTAPPLYFIELDMODE_LONG_FIELDMODE!= 0 ))||is_error_pending()){
if(qbevent){evnt(15641);if(r)goto S_18150;}
error_track_line(15642,0,NULL);
do{
SUB_GIVE_ERROR(qbs_new_txt_len("Only one TYPE member array field mode may be specified",54));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15642);}while(r);
error_track_line(15643,0,NULL);
do{
goto exit_subfunc;
if(!qbevent)break;evnt(15643);}while(r);
error_track_line(15644,0,NULL);
}
error_track_line(15645,0,NULL);
do{
*_SUB_UDTAPPLYFIELDMODE_LONG_FIELDMODE=*_SUB_UDTAPPLYFIELDMODE_LONG_PARSED_MODE;
if(!qbevent)break;evnt(15645);}while(r);
exit_subfunc:;
free_mem_lock(sf_mem_lock);
#include "free14.txt"
if ((tmp_mem_static_pointer>=mem_static)&&(tmp_mem_static_pointer<=mem_static_limit)) mem_static_pointer=tmp_mem_static_pointer; else mem_static_pointer=mem_static;
cmem_sp=tmp_cmem_sp;
}
