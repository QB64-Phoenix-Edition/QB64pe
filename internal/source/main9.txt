void SUB_REGISTERSFDYNMODE(int32*_SUB_REGISTERSFDYNMODE_LONG_SID,int16*_SUB_REGISTERSFDYNMODE_INTEGER_SARG,int16*_SUB_REGISTERSFDYNMODE_INTEGER_SELE,int16*_SUB_REGISTERSFDYNMODE_INTEGER_SDYN){
qbs *tqbs;
ptrszint tmp_long;
int32 tmp_fileno;
uint32 qbs_tmp_base=qbs_tmp_list_nexti;
uint8 *tmp_mem_static_pointer=mem_static_pointer;
uint32 tmp_cmem_sp=cmem_sp;
#include "data9.txt"
mem_lock *sf_mem_lock;
new_mem_lock();
sf_mem_lock=mem_lock_tmp;
sf_mem_lock->type=3;
if (is_error_pending()) goto exit_subfunc;
error_track_line(14574,0,NULL);
do{
if(!qbevent)break;evnt(14574);}while(r);
error_track_line(14576,0,NULL);
S_17175:;
if ((-(*_SUB_REGISTERSFDYNMODE_INTEGER_SDYN<= 0 ))||is_error_pending()){
if(qbevent){evnt(14576);if(r)goto S_17175;}
error_track_line(14576,0,NULL);
do{
goto exit_subfunc;
if(!qbevent)break;evnt(14576);}while(r);
error_track_line(14576,0,NULL);
}
error_track_line(14577,0,NULL);
do{
*__INTEGER_SFLISTN=*__INTEGER_SFLISTN+ 1 ;
if(!qbevent)break;evnt(14577);}while(r);
error_track_line(14578,0,NULL);
do{
*_SUB_REGISTERSFDYNMODE_LONG_UB_SF=func_ubound(__ARRAY_LONG_SFIDLIST,1,1);
if(!qbevent)break;evnt(14578);}while(r);
error_track_line(14579,0,NULL);
S_17180:;
if ((-(*__INTEGER_SFLISTN>*_SUB_REGISTERSFDYNMODE_LONG_UB_SF))||is_error_pending()){
if(qbevent){evnt(14579);if(r)goto S_17180;}
error_track_line(14580,0,NULL);
do{
if (__ARRAY_LONG_SFIDLIST[2]&2){
error(10);
}else{
static int64 alloc_new_desc[8];
static uint64 alloc_req_elems;
static uint64 alloc_req_bytes;
static uint64 alloc_dim_value;
static uint64 alloc_bytes_per_element;
((mem_lock*)((ptrszint*)__ARRAY_LONG_SFIDLIST)[8])->id=(++mem_lock_id);
static ptrszint preserve_old_ptr;
static ptrszint preserve_old_flags;
static uint64 preserve_old_total;
static uint64 preserve_new_total;
static uint64 preserve_copy_count;
static ptrszint preserve_new_ptr;
if (__ARRAY_LONG_SFIDLIST[2]&1){
preserve_old_ptr=__ARRAY_LONG_SFIDLIST[0];
preserve_old_flags=__ARRAY_LONG_SFIDLIST[2];
preserve_old_total=__ARRAY_LONG_SFIDLIST[5];
}else{
preserve_old_total=0; preserve_old_ptr=0; preserve_old_flags=0; preserve_new_total=0; preserve_copy_count=0;
}
alloc_new_desc[4]= 0 ;
alloc_new_desc[5]=(*_SUB_REGISTERSFDYNMODE_LONG_UB_SF+ 1000 )-alloc_new_desc[4]+1;
if (alloc_new_desc[5]<=0) error(5);
alloc_new_desc[6]=1;
alloc_req_elems=1;
alloc_bytes_per_element=(uint64)(4);
if (!alloc_bytes_per_element) error(257);
if (((int64)((ptrszint)alloc_new_desc[4]))!=alloc_new_desc[4]) error(257);
if (alloc_new_desc[5]<0) error(257);
if (((int64)((ptrszint)alloc_new_desc[5]))!=alloc_new_desc[5]) error(257);
if (alloc_new_desc[6]<0) error(257);
if (((int64)((ptrszint)alloc_new_desc[6]))!=alloc_new_desc[6]) error(257);
alloc_dim_value=(uint64)alloc_new_desc[5];
if (alloc_dim_value){
if (alloc_req_elems>(18446744073709551615ull/alloc_dim_value)) error(257);
alloc_req_elems*=alloc_dim_value;
}
if (alloc_req_elems>(18446744073709551615ull/alloc_bytes_per_element)) error(257);
alloc_req_bytes=alloc_req_elems*alloc_bytes_per_element;
if (((uint64)((ptrszint)alloc_req_elems))!=alloc_req_elems) error(257);
if (((uint64)((size_t)alloc_req_bytes))!=alloc_req_bytes) error(257);
preserve_new_total=alloc_req_elems;
preserve_copy_count=preserve_old_total;
if (preserve_new_total<preserve_copy_count) preserve_copy_count=preserve_new_total;
if (__ARRAY_LONG_SFIDLIST[2]&4){
if (preserve_old_total){
preserve_new_ptr=(ptrszint)cmem_dynamic_malloc((size_t)alloc_req_bytes);
if (!preserve_new_ptr) error(257);
memset((void*)(preserve_new_ptr),0,(size_t)alloc_req_bytes);
if (preserve_copy_count) memcpy((void*)(preserve_new_ptr),(void*)(preserve_old_ptr),preserve_copy_count*4);
if (preserve_old_flags&4){
cmem_dynamic_free((uint8*)(preserve_old_ptr));
}else{
free((void*)(preserve_old_ptr));
}
__ARRAY_LONG_SFIDLIST[0]=preserve_new_ptr;
}else{
__ARRAY_LONG_SFIDLIST[0]=(ptrszint)cmem_dynamic_malloc((size_t)alloc_req_bytes);
if (!__ARRAY_LONG_SFIDLIST[0]) error(257);
memset((void*)(__ARRAY_LONG_SFIDLIST[0]),0,(size_t)alloc_req_bytes);
}
}else{
if (preserve_old_total){
preserve_new_ptr=(ptrszint)calloc((size_t)alloc_req_bytes,1);
if (!preserve_new_ptr) error(257);
if (preserve_copy_count) memcpy((void*)(preserve_new_ptr),(void*)(preserve_old_ptr),preserve_copy_count*4);
if (preserve_old_flags&4){
cmem_dynamic_free((uint8*)(preserve_old_ptr));
}else{
free((void*)(preserve_old_ptr));
}
__ARRAY_LONG_SFIDLIST[0]=preserve_new_ptr;
}else{
__ARRAY_LONG_SFIDLIST[0]=(ptrszint)calloc((size_t)alloc_req_bytes,1);
if (!__ARRAY_LONG_SFIDLIST[0]) error(257);
}
}
__ARRAY_LONG_SFIDLIST[2]|=1;
__ARRAY_LONG_SFIDLIST[4]=alloc_new_desc[4];
__ARRAY_LONG_SFIDLIST[5]=alloc_new_desc[5];
__ARRAY_LONG_SFIDLIST[6]=alloc_new_desc[6];
__ARRAY_LONG_SFIDLIST[7]=alloc_new_desc[7];
}
if(!qbevent)break;evnt(14580);}while(r);
error_track_line(14581,0,NULL);
do{
if (__ARRAY_INTEGER_SFARGLIST[2]&2){
error(10);
}else{
static int64 alloc_new_desc[8];
static uint64 alloc_req_elems;
static uint64 alloc_req_bytes;
static uint64 alloc_dim_value;
static uint64 alloc_bytes_per_element;
((mem_lock*)((ptrszint*)__ARRAY_INTEGER_SFARGLIST)[8])->id=(++mem_lock_id);
static ptrszint preserve_old_ptr;
static ptrszint preserve_old_flags;
static uint64 preserve_old_total;
static uint64 preserve_new_total;
static uint64 preserve_copy_count;
static ptrszint preserve_new_ptr;
if (__ARRAY_INTEGER_SFARGLIST[2]&1){
preserve_old_ptr=__ARRAY_INTEGER_SFARGLIST[0];
preserve_old_flags=__ARRAY_INTEGER_SFARGLIST[2];
preserve_old_total=__ARRAY_INTEGER_SFARGLIST[5];
}else{
preserve_old_total=0; preserve_old_ptr=0; preserve_old_flags=0; preserve_new_total=0; preserve_copy_count=0;
}
alloc_new_desc[4]= 0 ;
alloc_new_desc[5]=(*_SUB_REGISTERSFDYNMODE_LONG_UB_SF+ 1000 )-alloc_new_desc[4]+1;
if (alloc_new_desc[5]<=0) error(5);
alloc_new_desc[6]=1;
alloc_req_elems=1;
alloc_bytes_per_element=(uint64)(2);
if (!alloc_bytes_per_element) error(257);
if (((int64)((ptrszint)alloc_new_desc[4]))!=alloc_new_desc[4]) error(257);
if (alloc_new_desc[5]<0) error(257);
if (((int64)((ptrszint)alloc_new_desc[5]))!=alloc_new_desc[5]) error(257);
if (alloc_new_desc[6]<0) error(257);
if (((int64)((ptrszint)alloc_new_desc[6]))!=alloc_new_desc[6]) error(257);
alloc_dim_value=(uint64)alloc_new_desc[5];
if (alloc_dim_value){
if (alloc_req_elems>(18446744073709551615ull/alloc_dim_value)) error(257);
alloc_req_elems*=alloc_dim_value;
}
if (alloc_req_elems>(18446744073709551615ull/alloc_bytes_per_element)) error(257);
alloc_req_bytes=alloc_req_elems*alloc_bytes_per_element;
if (((uint64)((ptrszint)alloc_req_elems))!=alloc_req_elems) error(257);
if (((uint64)((size_t)alloc_req_bytes))!=alloc_req_bytes) error(257);
preserve_new_total=alloc_req_elems;
preserve_copy_count=preserve_old_total;
if (preserve_new_total<preserve_copy_count) preserve_copy_count=preserve_new_total;
if (__ARRAY_INTEGER_SFARGLIST[2]&4){
if (preserve_old_total){
preserve_new_ptr=(ptrszint)cmem_dynamic_malloc((size_t)alloc_req_bytes);
if (!preserve_new_ptr) error(257);
memset((void*)(preserve_new_ptr),0,(size_t)alloc_req_bytes);
if (preserve_copy_count) memcpy((void*)(preserve_new_ptr),(void*)(preserve_old_ptr),preserve_copy_count*2);
if (preserve_old_flags&4){
cmem_dynamic_free((uint8*)(preserve_old_ptr));
}else{
free((void*)(preserve_old_ptr));
}
__ARRAY_INTEGER_SFARGLIST[0]=preserve_new_ptr;
}else{
__ARRAY_INTEGER_SFARGLIST[0]=(ptrszint)cmem_dynamic_malloc((size_t)alloc_req_bytes);
if (!__ARRAY_INTEGER_SFARGLIST[0]) error(257);
memset((void*)(__ARRAY_INTEGER_SFARGLIST[0]),0,(size_t)alloc_req_bytes);
}
}else{
if (preserve_old_total){
preserve_new_ptr=(ptrszint)calloc((size_t)alloc_req_bytes,1);
if (!preserve_new_ptr) error(257);
if (preserve_copy_count) memcpy((void*)(preserve_new_ptr),(void*)(preserve_old_ptr),preserve_copy_count*2);
if (preserve_old_flags&4){
cmem_dynamic_free((uint8*)(preserve_old_ptr));
}else{
free((void*)(preserve_old_ptr));
}
__ARRAY_INTEGER_SFARGLIST[0]=preserve_new_ptr;
}else{
__ARRAY_INTEGER_SFARGLIST[0]=(ptrszint)calloc((size_t)alloc_req_bytes,1);
if (!__ARRAY_INTEGER_SFARGLIST[0]) error(257);
}
}
__ARRAY_INTEGER_SFARGLIST[2]|=1;
__ARRAY_INTEGER_SFARGLIST[4]=alloc_new_desc[4];
__ARRAY_INTEGER_SFARGLIST[5]=alloc_new_desc[5];
__ARRAY_INTEGER_SFARGLIST[6]=alloc_new_desc[6];
__ARRAY_INTEGER_SFARGLIST[7]=alloc_new_desc[7];
}
if(!qbevent)break;evnt(14581);}while(r);
error_track_line(14582,0,NULL);
do{
if (__ARRAY_INTEGER_SFELELIST[2]&2){
error(10);
}else{
static int64 alloc_new_desc[8];
static uint64 alloc_req_elems;
static uint64 alloc_req_bytes;
static uint64 alloc_dim_value;
static uint64 alloc_bytes_per_element;
((mem_lock*)((ptrszint*)__ARRAY_INTEGER_SFELELIST)[8])->id=(++mem_lock_id);
static ptrszint preserve_old_ptr;
static ptrszint preserve_old_flags;
static uint64 preserve_old_total;
static uint64 preserve_new_total;
static uint64 preserve_copy_count;
static ptrszint preserve_new_ptr;
if (__ARRAY_INTEGER_SFELELIST[2]&1){
preserve_old_ptr=__ARRAY_INTEGER_SFELELIST[0];
preserve_old_flags=__ARRAY_INTEGER_SFELELIST[2];
preserve_old_total=__ARRAY_INTEGER_SFELELIST[5];
}else{
preserve_old_total=0; preserve_old_ptr=0; preserve_old_flags=0; preserve_new_total=0; preserve_copy_count=0;
}
alloc_new_desc[4]= 0 ;
alloc_new_desc[5]=(*_SUB_REGISTERSFDYNMODE_LONG_UB_SF+ 1000 )-alloc_new_desc[4]+1;
if (alloc_new_desc[5]<=0) error(5);
alloc_new_desc[6]=1;
alloc_req_elems=1;
alloc_bytes_per_element=(uint64)(2);
if (!alloc_bytes_per_element) error(257);
if (((int64)((ptrszint)alloc_new_desc[4]))!=alloc_new_desc[4]) error(257);
if (alloc_new_desc[5]<0) error(257);
if (((int64)((ptrszint)alloc_new_desc[5]))!=alloc_new_desc[5]) error(257);
if (alloc_new_desc[6]<0) error(257);
if (((int64)((ptrszint)alloc_new_desc[6]))!=alloc_new_desc[6]) error(257);
alloc_dim_value=(uint64)alloc_new_desc[5];
if (alloc_dim_value){
if (alloc_req_elems>(18446744073709551615ull/alloc_dim_value)) error(257);
alloc_req_elems*=alloc_dim_value;
}
if (alloc_req_elems>(18446744073709551615ull/alloc_bytes_per_element)) error(257);
alloc_req_bytes=alloc_req_elems*alloc_bytes_per_element;
if (((uint64)((ptrszint)alloc_req_elems))!=alloc_req_elems) error(257);
if (((uint64)((size_t)alloc_req_bytes))!=alloc_req_bytes) error(257);
preserve_new_total=alloc_req_elems;
preserve_copy_count=preserve_old_total;
if (preserve_new_total<preserve_copy_count) preserve_copy_count=preserve_new_total;
if (__ARRAY_INTEGER_SFELELIST[2]&4){
if (preserve_old_total){
preserve_new_ptr=(ptrszint)cmem_dynamic_malloc((size_t)alloc_req_bytes);
if (!preserve_new_ptr) error(257);
memset((void*)(preserve_new_ptr),0,(size_t)alloc_req_bytes);
if (preserve_copy_count) memcpy((void*)(preserve_new_ptr),(void*)(preserve_old_ptr),preserve_copy_count*2);
if (preserve_old_flags&4){
cmem_dynamic_free((uint8*)(preserve_old_ptr));
}else{
free((void*)(preserve_old_ptr));
}
__ARRAY_INTEGER_SFELELIST[0]=preserve_new_ptr;
}else{
__ARRAY_INTEGER_SFELELIST[0]=(ptrszint)cmem_dynamic_malloc((size_t)alloc_req_bytes);
if (!__ARRAY_INTEGER_SFELELIST[0]) error(257);
memset((void*)(__ARRAY_INTEGER_SFELELIST[0]),0,(size_t)alloc_req_bytes);
}
}else{
if (preserve_old_total){
preserve_new_ptr=(ptrszint)calloc((size_t)alloc_req_bytes,1);
if (!preserve_new_ptr) error(257);
if (preserve_copy_count) memcpy((void*)(preserve_new_ptr),(void*)(preserve_old_ptr),preserve_copy_count*2);
if (preserve_old_flags&4){
cmem_dynamic_free((uint8*)(preserve_old_ptr));
}else{
free((void*)(preserve_old_ptr));
}
__ARRAY_INTEGER_SFELELIST[0]=preserve_new_ptr;
}else{
__ARRAY_INTEGER_SFELELIST[0]=(ptrszint)calloc((size_t)alloc_req_bytes,1);
if (!__ARRAY_INTEGER_SFELELIST[0]) error(257);
}
}
__ARRAY_INTEGER_SFELELIST[2]|=1;
__ARRAY_INTEGER_SFELELIST[4]=alloc_new_desc[4];
__ARRAY_INTEGER_SFELELIST[5]=alloc_new_desc[5];
__ARRAY_INTEGER_SFELELIST[6]=alloc_new_desc[6];
__ARRAY_INTEGER_SFELELIST[7]=alloc_new_desc[7];
}
if(!qbevent)break;evnt(14582);}while(r);
error_track_line(14583,0,NULL);
do{
if (__ARRAY_INTEGER_SFDYNMODELIST[2]&2){
error(10);
}else{
static int64 alloc_new_desc[8];
static uint64 alloc_req_elems;
static uint64 alloc_req_bytes;
static uint64 alloc_dim_value;
static uint64 alloc_bytes_per_element;
((mem_lock*)((ptrszint*)__ARRAY_INTEGER_SFDYNMODELIST)[8])->id=(++mem_lock_id);
static ptrszint preserve_old_ptr;
static ptrszint preserve_old_flags;
static uint64 preserve_old_total;
static uint64 preserve_new_total;
static uint64 preserve_copy_count;
static ptrszint preserve_new_ptr;
if (__ARRAY_INTEGER_SFDYNMODELIST[2]&1){
preserve_old_ptr=__ARRAY_INTEGER_SFDYNMODELIST[0];
preserve_old_flags=__ARRAY_INTEGER_SFDYNMODELIST[2];
preserve_old_total=__ARRAY_INTEGER_SFDYNMODELIST[5];
}else{
preserve_old_total=0; preserve_old_ptr=0; preserve_old_flags=0; preserve_new_total=0; preserve_copy_count=0;
}
alloc_new_desc[4]= 0 ;
alloc_new_desc[5]=(*_SUB_REGISTERSFDYNMODE_LONG_UB_SF+ 1000 )-alloc_new_desc[4]+1;
if (alloc_new_desc[5]<=0) error(5);
alloc_new_desc[6]=1;
alloc_req_elems=1;
alloc_bytes_per_element=(uint64)(2);
if (!alloc_bytes_per_element) error(257);
if (((int64)((ptrszint)alloc_new_desc[4]))!=alloc_new_desc[4]) error(257);
if (alloc_new_desc[5]<0) error(257);
if (((int64)((ptrszint)alloc_new_desc[5]))!=alloc_new_desc[5]) error(257);
if (alloc_new_desc[6]<0) error(257);
if (((int64)((ptrszint)alloc_new_desc[6]))!=alloc_new_desc[6]) error(257);
alloc_dim_value=(uint64)alloc_new_desc[5];
if (alloc_dim_value){
if (alloc_req_elems>(18446744073709551615ull/alloc_dim_value)) error(257);
alloc_req_elems*=alloc_dim_value;
}
if (alloc_req_elems>(18446744073709551615ull/alloc_bytes_per_element)) error(257);
alloc_req_bytes=alloc_req_elems*alloc_bytes_per_element;
if (((uint64)((ptrszint)alloc_req_elems))!=alloc_req_elems) error(257);
if (((uint64)((size_t)alloc_req_bytes))!=alloc_req_bytes) error(257);
preserve_new_total=alloc_req_elems;
preserve_copy_count=preserve_old_total;
if (preserve_new_total<preserve_copy_count) preserve_copy_count=preserve_new_total;
if (__ARRAY_INTEGER_SFDYNMODELIST[2]&4){
if (preserve_old_total){
preserve_new_ptr=(ptrszint)cmem_dynamic_malloc((size_t)alloc_req_bytes);
if (!preserve_new_ptr) error(257);
memset((void*)(preserve_new_ptr),0,(size_t)alloc_req_bytes);
if (preserve_copy_count) memcpy((void*)(preserve_new_ptr),(void*)(preserve_old_ptr),preserve_copy_count*2);
if (preserve_old_flags&4){
cmem_dynamic_free((uint8*)(preserve_old_ptr));
}else{
free((void*)(preserve_old_ptr));
}
__ARRAY_INTEGER_SFDYNMODELIST[0]=preserve_new_ptr;
}else{
__ARRAY_INTEGER_SFDYNMODELIST[0]=(ptrszint)cmem_dynamic_malloc((size_t)alloc_req_bytes);
if (!__ARRAY_INTEGER_SFDYNMODELIST[0]) error(257);
memset((void*)(__ARRAY_INTEGER_SFDYNMODELIST[0]),0,(size_t)alloc_req_bytes);
}
}else{
if (preserve_old_total){
preserve_new_ptr=(ptrszint)calloc((size_t)alloc_req_bytes,1);
if (!preserve_new_ptr) error(257);
if (preserve_copy_count) memcpy((void*)(preserve_new_ptr),(void*)(preserve_old_ptr),preserve_copy_count*2);
if (preserve_old_flags&4){
cmem_dynamic_free((uint8*)(preserve_old_ptr));
}else{
free((void*)(preserve_old_ptr));
}
__ARRAY_INTEGER_SFDYNMODELIST[0]=preserve_new_ptr;
}else{
__ARRAY_INTEGER_SFDYNMODELIST[0]=(ptrszint)calloc((size_t)alloc_req_bytes,1);
if (!__ARRAY_INTEGER_SFDYNMODELIST[0]) error(257);
}
}
__ARRAY_INTEGER_SFDYNMODELIST[2]|=1;
__ARRAY_INTEGER_SFDYNMODELIST[4]=alloc_new_desc[4];
__ARRAY_INTEGER_SFDYNMODELIST[5]=alloc_new_desc[5];
__ARRAY_INTEGER_SFDYNMODELIST[6]=alloc_new_desc[6];
__ARRAY_INTEGER_SFDYNMODELIST[7]=alloc_new_desc[7];
}
if(!qbevent)break;evnt(14583);}while(r);
error_track_line(14584,0,NULL);
}
error_track_line(14585,0,NULL);
do{
tmp_long=array_check((*__INTEGER_SFLISTN)-__ARRAY_LONG_SFIDLIST[4],__ARRAY_LONG_SFIDLIST[5]);
if (!is_error_pending()) ((int32*)(__ARRAY_LONG_SFIDLIST[0]))[tmp_long]=*_SUB_REGISTERSFDYNMODE_LONG_SID;
if(!qbevent)break;evnt(14585);}while(r);
error_track_line(14586,0,NULL);
do{
tmp_long=array_check((*__INTEGER_SFLISTN)-__ARRAY_INTEGER_SFARGLIST[4],__ARRAY_INTEGER_SFARGLIST[5]);
if (!is_error_pending()) ((int16*)(__ARRAY_INTEGER_SFARGLIST[0]))[tmp_long]=*_SUB_REGISTERSFDYNMODE_INTEGER_SARG;
if(!qbevent)break;evnt(14586);}while(r);
error_track_line(14587,0,NULL);
do{
tmp_long=array_check((*__INTEGER_SFLISTN)-__ARRAY_INTEGER_SFELELIST[4],__ARRAY_INTEGER_SFELELIST[5]);
if (!is_error_pending()) ((int16*)(__ARRAY_INTEGER_SFELELIST[0]))[tmp_long]=*_SUB_REGISTERSFDYNMODE_INTEGER_SELE;
if(!qbevent)break;evnt(14587);}while(r);
error_track_line(14588,0,NULL);
do{
tmp_long=array_check((*__INTEGER_SFLISTN)-__ARRAY_INTEGER_SFDYNMODELIST[4],__ARRAY_INTEGER_SFDYNMODELIST[5]);
if (!is_error_pending()) ((int16*)(__ARRAY_INTEGER_SFDYNMODELIST[0]))[tmp_long]=*_SUB_REGISTERSFDYNMODE_INTEGER_SDYN;
if(!qbevent)break;evnt(14588);}while(r);
exit_subfunc:;
free_mem_lock(sf_mem_lock);
#include "free9.txt"
if ((tmp_mem_static_pointer>=mem_static)&&(tmp_mem_static_pointer<=mem_static_limit)) mem_static_pointer=tmp_mem_static_pointer; else mem_static_pointer=mem_static;
cmem_sp=tmp_cmem_sp;
}
