qbs* FUNC_UDTDYNARRAYBYTESEXPR(qbs*_FUNC_UDTDYNARRAYBYTESEXPR_STRING_DESC_EXPR,int32*_FUNC_UDTDYNARRAYBYTESEXPR_LONG_ELEM_BYTES){
qbs *tqbs;
ptrszint tmp_long;
int32 tmp_fileno;
uint32 qbs_tmp_base=qbs_tmp_list_nexti;
uint8 *tmp_mem_static_pointer=mem_static_pointer;
uint32 tmp_cmem_sp=cmem_sp;
#include "data36.txt"
mem_lock *sf_mem_lock;
new_mem_lock();
sf_mem_lock=mem_lock_tmp;
sf_mem_lock->type=3;
if (is_error_pending()) goto exit_subfunc;
error_track_line(16612,0,NULL);
do{
if(!qbevent)break;evnt(16612);}while(r);
error_track_line(16613,0,NULL);
do{
if(!qbevent)break;evnt(16613);}while(r);
error_track_line(16615,0,NULL);
S_19106:;
if ((-(*_FUNC_UDTDYNARRAYBYTESEXPR_LONG_ELEM_BYTES<= 0 ))||is_error_pending()){
if(qbevent){evnt(16615);if(r)goto S_19106;}
error_track_line(16615,0,NULL);
do{
SUB_GIVE_ERROR(qbs_new_txt_len("Invalid TYPE member array metadata",34));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16615);}while(r);
error_track_line(16615,0,NULL);
do{
goto exit_subfunc;
if(!qbevent)break;evnt(16615);}while(r);
error_track_line(16615,0,NULL);
}
error_track_line(16617,0,NULL);
do{
qbs_set(_FUNC_UDTDYNARRAYBYTESEXPR_STRING_HELPER_NAME,qbs_add(qbs_new_txt_len("udt_dyn_array_bytes_",20),qbs__tostr((int32)(FUNC_UNIQUENUMBER()),NULL,0)));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16617);}while(r);
error_track_line(16618,0,NULL);
do{
qbs_set(_FUNC_UDTDYNARRAYBYTESEXPR_STRING_ELEM_TEXT,qbs_ltrim(qbs_str((int32)(*_FUNC_UDTDYNARRAYBYTESEXPR_LONG_ELEM_BYTES))));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16618);}while(r);
error_track_line(16620,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2808=*__LONG_GLOBTXTBUF),qbs_add(qbs_add(qbs_new_txt_len("static ptrszint ",16),_FUNC_UDTDYNARRAYBYTESEXPR_STRING_HELPER_NAME),qbs_new_txt_len("(ptrszint *qb_desc){",20)));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16620);}while(r);
error_track_line(16621,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2809=*__LONG_GLOBTXTBUF),qbs_new_txt_len("ptrszint qb_i;",14));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16621);}while(r);
error_track_line(16622,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2810=*__LONG_GLOBTXTBUF),qbs_new_txt_len("ptrszint qb_arg;",16));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16622);}while(r);
error_track_line(16623,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2811=*__LONG_GLOBTXTBUF),qbs_new_txt_len("ptrszint qb_count;",18));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16623);}while(r);
error_track_line(16624,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2812=*__LONG_GLOBTXTBUF),qbs_new_txt_len("uint64 qb_total=1;",18));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16624);}while(r);
error_track_line(16625,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2813=*__LONG_GLOBTXTBUF),qbs_new_txt_len("if ((!qb_desc)||(qb_desc[3]<=0)){error(9); return 0;}",53));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16625);}while(r);
error_track_line(16626,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2814=*__LONG_GLOBTXTBUF),qbs_new_txt_len("for(qb_i=1; qb_i<=qb_desc[3]; qb_i++){",38));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16626);}while(r);
error_track_line(16627,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2815=*__LONG_GLOBTXTBUF),qbs_new_txt_len("qb_arg=(qb_desc[3]-qb_i)*4+4;",29));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16627);}while(r);
error_track_line(16628,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2816=*__LONG_GLOBTXTBUF),qbs_new_txt_len("qb_count=qb_desc[qb_arg+1];",27));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16628);}while(r);
error_track_line(16629,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2817=*__LONG_GLOBTXTBUF),qbs_new_txt_len("if (qb_count<0){error(9); return 0;}",36));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16629);}while(r);
error_track_line(16630,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2818=*__LONG_GLOBTXTBUF),qbs_new_txt_len("if (qb_count&&qb_total>(18446744073709551615ull/(uint64)qb_count)){error(257); return 0;}",89));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16630);}while(r);
error_track_line(16631,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2819=*__LONG_GLOBTXTBUF),qbs_new_txt_len("qb_total*=(uint64)qb_count;",27));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16631);}while(r);
error_track_line(16632,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2820=*__LONG_GLOBTXTBUF),qbs_new_txt_len("}",1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16632);}while(r);
error_track_line(16633,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2821=*__LONG_GLOBTXTBUF),qbs_add(qbs_add(qbs_new_txt_len("if (qb_total>(18446744073709551615ull/(uint64)",46),_FUNC_UDTDYNARRAYBYTESEXPR_STRING_ELEM_TEXT),qbs_new_txt_len(")){error(257); return 0;}",25)));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16633);}while(r);
error_track_line(16634,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2822=*__LONG_GLOBTXTBUF),qbs_add(qbs_add(qbs_new_txt_len("return (ptrszint)(qb_total*(uint64)",35),_FUNC_UDTDYNARRAYBYTESEXPR_STRING_ELEM_TEXT),qbs_new_txt_len(");",2)));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16634);}while(r);
error_track_line(16635,0,NULL);
do{
SUB_WRITEBUFLINE(&(pass2823=*__LONG_GLOBTXTBUF),qbs_new_txt_len("}",1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16635);}while(r);
error_track_line(16637,0,NULL);
do{
qbs_set(_FUNC_UDTDYNARRAYBYTESEXPR_STRING_UDTDYNARRAYBYTESEXPR,qbs_add(qbs_add(qbs_add(_FUNC_UDTDYNARRAYBYTESEXPR_STRING_HELPER_NAME,qbs_new_txt_len("(",1)),_FUNC_UDTDYNARRAYBYTESEXPR_STRING_DESC_EXPR),qbs_new_txt_len(")",1)));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(16637);}while(r);
exit_subfunc:;
free_mem_lock(sf_mem_lock);
#include "free36.txt"
if ((tmp_mem_static_pointer>=mem_static)&&(tmp_mem_static_pointer<=mem_static_limit)) mem_static_pointer=tmp_mem_static_pointer; else mem_static_pointer=mem_static;
cmem_sp=tmp_cmem_sp;
qbs_maketmp(_FUNC_UDTDYNARRAYBYTESEXPR_STRING_UDTDYNARRAYBYTESEXPR);return _FUNC_UDTDYNARRAYBYTESEXPR_STRING_UDTDYNARRAYBYTESEXPR;
}
