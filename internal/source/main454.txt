void SUB_HELP_ADDTXT(qbs*_SUB_HELP_ADDTXT_STRING_T,int32*_SUB_HELP_ADDTXT_LONG_COL,int32*_SUB_HELP_ADDTXT_LONG_LINK){
qbs *tqbs;
ptrszint tmp_long;
int32 tmp_fileno;
uint32 qbs_tmp_base=qbs_tmp_list_nexti;
uint8 *tmp_mem_static_pointer=mem_static_pointer;
uint32 tmp_cmem_sp=cmem_sp;
#include "data454.txt"
mem_lock *sf_mem_lock;
new_mem_lock();
sf_mem_lock=mem_lock_tmp;
sf_mem_lock->type=3;
if (is_error_pending()) goto exit_subfunc;
error_track_line(28345,138,"wiki_methods.bas");
S_68138:;
if ((qbs_cleanup(qbs_tmp_base,qbs_equal(_SUB_HELP_ADDTXT_STRING_T,qbs_new_txt_len("",0))))||is_error_pending()){
if(qbevent){evnt(28345,138,"wiki_methods.bas");if(r)goto S_68138;}
error_track_line(28345,138,"wiki_methods.bas");
do{
goto exit_subfunc;
if(!qbevent)break;evnt(28345,138,"wiki_methods.bas");}while(r);
error_track_line(28345,138,"wiki_methods.bas");
}
error_track_line(28345,139,"wiki_methods.bas");
S_68141:;
if ((-(*__LONG_HELP_CHKBLANK!= 0 ))||is_error_pending()){
if(qbevent){evnt(28345,139,"wiki_methods.bas");if(r)goto S_68141;}
error_track_line(28345,139,"wiki_methods.bas");
do{
SUB_HELP_CHECKBLANKLINE();
if(!qbevent)break;evnt(28345,139,"wiki_methods.bas");}while(r);
error_track_line(28345,139,"wiki_methods.bas");
}
error_track_line(28345,141,"wiki_methods.bas");
S_68144:;
fornext_value7861= 1 ;
fornext_finalvalue7861=_SUB_HELP_ADDTXT_STRING_T->len;
fornext_step7861= 1 ;
if (fornext_step7861<0) fornext_step_negative7861=1; else fornext_step_negative7861=0;
if (is_error_pending()) goto fornext_error7861;
goto fornext_entrylabel7861;
while(1){
fornext_value7861=fornext_step7861+(*_SUB_HELP_ADDTXT_LONG_I);
fornext_entrylabel7861:
*_SUB_HELP_ADDTXT_LONG_I=fornext_value7861;
qbs_cleanup(qbs_tmp_base,0);
if (fornext_step_negative7861){
if (fornext_value7861<fornext_finalvalue7861) break;
}else{
if (fornext_value7861>fornext_finalvalue7861) break;
}
fornext_error7861:;
if(qbevent){evnt(28345,141,"wiki_methods.bas");if(r)goto S_68144;}
error_track_line(28345,142,"wiki_methods.bas");
do{
*_SUB_HELP_ADDTXT_LONG_C=qbs_asc(_SUB_HELP_ADDTXT_STRING_T,*_SUB_HELP_ADDTXT_LONG_I);
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(28345,142,"wiki_methods.bas");}while(r);
error_track_line(28345,144,"wiki_methods.bas");
S_68146:;
if (((((-(*__LONG_HELP_LOCKPARSE== -1 ))|(-(*__LONG_HELP_LOCKPARSE== 0 ))))&(-(*__LONG_HELP_LOCKWRAP== 0 )))||is_error_pending()){
if(qbevent){evnt(28345,144,"wiki_methods.bas");if(r)goto S_68146;}
error_track_line(28345,146,"wiki_methods.bas");
S_68147:;
if ((-(*_SUB_HELP_ADDTXT_LONG_C== 32 ))||is_error_pending()){
if(qbevent){evnt(28345,146,"wiki_methods.bas");if(r)goto S_68147;}
error_track_line(28345,147,"wiki_methods.bas");
S_68148:;
if ((-(*__LONG_HELP_POS==*__LONG_HELP_WW))||is_error_pending()){
if(qbevent){evnt(28345,147,"wiki_methods.bas");if(r)goto S_68148;}
error_track_line(28345,147,"wiki_methods.bas");
do{
SUB_HELP_NEWLINE();
if(!qbevent)break;evnt(28345,147,"wiki_methods.bas");}while(r);
error_track_line(28345,147,"wiki_methods.bas");
do{
goto fornext_continue_7860;
if(!qbevent)break;evnt(28345,147,"wiki_methods.bas");}while(r);
error_track_line(28345,147,"wiki_methods.bas");
}
error_track_line(28345,149,"wiki_methods.bas");
do{
*__LONG_HELP_TXT_LEN=*__LONG_HELP_TXT_LEN+ 1 ;
if(!qbevent)break;evnt(28345,149,"wiki_methods.bas");}while(r);
error_track_line(28345,149,"wiki_methods.bas");
do{
tqbs=__STRING_HELP_TXT; if (!is_error_pending()){
tmp_fileno=*__LONG_HELP_TXT_LEN; if (!is_error_pending()){
tmp_long= 32 ; if (!is_error_pending()){
if ((tmp_fileno>0)&&(tmp_fileno<=tqbs->len)){tqbs->chr[tmp_fileno-1]=tmp_long;}else{error(5);}
}}}
if(!qbevent)break;evnt(28345,149,"wiki_methods.bas");}while(r);
error_track_line(28345,150,"wiki_methods.bas");
do{
*__LONG_HELP_TXT_LEN=*__LONG_HELP_TXT_LEN+ 1 ;
if(!qbevent)break;evnt(28345,150,"wiki_methods.bas");}while(r);
error_track_line(28345,150,"wiki_methods.bas");
do{
tqbs=__STRING_HELP_TXT; if (!is_error_pending()){
tmp_fileno=*__LONG_HELP_TXT_LEN; if (!is_error_pending()){
tmp_long=*_SUB_HELP_ADDTXT_LONG_COL+(*__LONG_HELP_BG_COL* 16 ); if (!is_error_pending()){
if ((tmp_fileno>0)&&(tmp_fileno<=tqbs->len)){tqbs->chr[tmp_fileno-1]=tmp_long;}else{error(5);}
}}}
if(!qbevent)break;evnt(28345,150,"wiki_methods.bas");}while(r);
error_track_line(28345,151,"wiki_methods.bas");
do{
*__LONG_HELP_TXT_LEN=*__LONG_HELP_TXT_LEN+ 1 ;
if(!qbevent)break;evnt(28345,151,"wiki_methods.bas");}while(r);
error_track_line(28345,151,"wiki_methods.bas");
do{
tqbs=__STRING_HELP_TXT; if (!is_error_pending()){
tmp_fileno=*__LONG_HELP_TXT_LEN; if (!is_error_pending()){
tmp_long=*_SUB_HELP_ADDTXT_LONG_LINK& 255 ; if (!is_error_pending()){
if ((tmp_fileno>0)&&(tmp_fileno<=tqbs->len)){tqbs->chr[tmp_fileno-1]=tmp_long;}else{error(5);}
}}}
if(!qbevent)break;evnt(28345,151,"wiki_methods.bas");}while(r);
error_track_line(28345,152,"wiki_methods.bas");
do{
*__LONG_HELP_TXT_LEN=*__LONG_HELP_TXT_LEN+ 1 ;
if(!qbevent)break;evnt(28345,152,"wiki_methods.bas");}while(r);
error_track_line(28345,152,"wiki_methods.bas");
do{
tqbs=__STRING_HELP_TXT; if (!is_error_pending()){
tmp_fileno=*__LONG_HELP_TXT_LEN; if (!is_error_pending()){
tmp_long=qb_safe_idiv(*_SUB_HELP_ADDTXT_LONG_LINK, 256 ); if (!is_error_pending()){
if ((tmp_fileno>0)&&(tmp_fileno<=tqbs->len)){tqbs->chr[tmp_fileno-1]=tmp_long;}else{error(5);}
}}}
if(!qbevent)break;evnt(28345,152,"wiki_methods.bas");}while(r);
error_track_line(28345,154,"wiki_methods.bas");
do{
*__LONG_HELP_WRAP_POS=*__LONG_HELP_TXT_LEN;
if(!qbevent)break;evnt(28345,154,"wiki_methods.bas");}while(r);
error_track_line(28345,155,"wiki_methods.bas");
do{
*__LONG_HELP_POS=*__LONG_HELP_POS+ 1 ;
if(!qbevent)break;evnt(28345,155,"wiki_methods.bas");}while(r);
error_track_line(28345,155,"wiki_methods.bas");
do{
goto fornext_continue_7860;
if(!qbevent)break;evnt(28345,155,"wiki_methods.bas");}while(r);
error_track_line(28345,156,"wiki_methods.bas");
}
error_track_line(28345,158,"wiki_methods.bas");
S_68164:;
if ((-(*__LONG_HELP_POS>*__LONG_HELP_WW))||is_error_pending()){
if(qbevent){evnt(28345,158,"wiki_methods.bas");if(r)goto S_68164;}
error_track_line(28345,159,"wiki_methods.bas");
S_68165:;
if ((*__LONG_HELP_WRAP_POS)||is_error_pending()){
if(qbevent){evnt(28345,159,"wiki_methods.bas");if(r)goto S_68165;}
error_track_line(28345,162,"wiki_methods.bas");
do{
qbs_set(_SUB_HELP_ADDTXT_STRING_B,func_mid(__STRING_HELP_TXT,*__LONG_HELP_WRAP_POS+ 1 ,*__LONG_HELP_TXT_LEN-*__LONG_HELP_WRAP_POS,0|1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(28345,162,"wiki_methods.bas");}while(r);
error_track_line(28345,164,"wiki_methods.bas");
do{
*__LONG_HELP_TXT_LEN=*__LONG_HELP_WRAP_POS;
if(!qbevent)break;evnt(28345,164,"wiki_methods.bas");}while(r);
error_track_line(28345,166,"wiki_methods.bas");
do{
SUB_HELP_NEWLINE();
if(!qbevent)break;evnt(28345,166,"wiki_methods.bas");}while(r);
error_track_line(28345,168,"wiki_methods.bas");
do{
sub_mid(__STRING_HELP_TXT,*__LONG_HELP_TXT_LEN+ 1 ,_SUB_HELP_ADDTXT_STRING_B->len,_SUB_HELP_ADDTXT_STRING_B,1);
if(!qbevent)break;evnt(28345,168,"wiki_methods.bas");}while(r);
error_track_line(28345,168,"wiki_methods.bas");
do{
*__LONG_HELP_TXT_LEN=*__LONG_HELP_TXT_LEN+_SUB_HELP_ADDTXT_STRING_B->len;
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(28345,168,"wiki_methods.bas");}while(r);
error_track_line(28345,170,"wiki_methods.bas");
do{
*__LONG_HELP_POS=*__LONG_HELP_POS+(qb_safe_idiv(_SUB_HELP_ADDTXT_STRING_B->len, 4 ));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(28345,170,"wiki_methods.bas");}while(r);
error_track_line(28345,171,"wiki_methods.bas");
}
error_track_line(28345,172,"wiki_methods.bas");
}
error_track_line(28345,174,"wiki_methods.bas");
}
error_track_line(28345,176,"wiki_methods.bas");
do{
*__LONG_HELP_TXT_LEN=*__LONG_HELP_TXT_LEN+ 1 ;
if(!qbevent)break;evnt(28345,176,"wiki_methods.bas");}while(r);
error_track_line(28345,176,"wiki_methods.bas");
do{
tqbs=__STRING_HELP_TXT; if (!is_error_pending()){
tmp_fileno=*__LONG_HELP_TXT_LEN; if (!is_error_pending()){
tmp_long=*_SUB_HELP_ADDTXT_LONG_C; if (!is_error_pending()){
if ((tmp_fileno>0)&&(tmp_fileno<=tqbs->len)){tqbs->chr[tmp_fileno-1]=tmp_long;}else{error(5);}
}}}
if(!qbevent)break;evnt(28345,176,"wiki_methods.bas");}while(r);
error_track_line(28345,177,"wiki_methods.bas");
do{
*__LONG_HELP_TXT_LEN=*__LONG_HELP_TXT_LEN+ 1 ;
if(!qbevent)break;evnt(28345,177,"wiki_methods.bas");}while(r);
error_track_line(28345,177,"wiki_methods.bas");
do{
tqbs=__STRING_HELP_TXT; if (!is_error_pending()){
tmp_fileno=*__LONG_HELP_TXT_LEN; if (!is_error_pending()){
tmp_long=*_SUB_HELP_ADDTXT_LONG_COL+(*__LONG_HELP_BG_COL* 16 ); if (!is_error_pending()){
if ((tmp_fileno>0)&&(tmp_fileno<=tqbs->len)){tqbs->chr[tmp_fileno-1]=tmp_long;}else{error(5);}
}}}
if(!qbevent)break;evnt(28345,177,"wiki_methods.bas");}while(r);
error_track_line(28345,178,"wiki_methods.bas");
do{
*__LONG_HELP_TXT_LEN=*__LONG_HELP_TXT_LEN+ 1 ;
if(!qbevent)break;evnt(28345,178,"wiki_methods.bas");}while(r);
error_track_line(28345,178,"wiki_methods.bas");
do{
tqbs=__STRING_HELP_TXT; if (!is_error_pending()){
tmp_fileno=*__LONG_HELP_TXT_LEN; if (!is_error_pending()){
tmp_long=*_SUB_HELP_ADDTXT_LONG_LINK& 255 ; if (!is_error_pending()){
if ((tmp_fileno>0)&&(tmp_fileno<=tqbs->len)){tqbs->chr[tmp_fileno-1]=tmp_long;}else{error(5);}
}}}
if(!qbevent)break;evnt(28345,178,"wiki_methods.bas");}while(r);
error_track_line(28345,179,"wiki_methods.bas");
do{
*__LONG_HELP_TXT_LEN=*__LONG_HELP_TXT_LEN+ 1 ;
if(!qbevent)break;evnt(28345,179,"wiki_methods.bas");}while(r);
error_track_line(28345,179,"wiki_methods.bas");
do{
tqbs=__STRING_HELP_TXT; if (!is_error_pending()){
tmp_fileno=*__LONG_HELP_TXT_LEN; if (!is_error_pending()){
tmp_long=qb_safe_idiv(*_SUB_HELP_ADDTXT_LONG_LINK, 256 ); if (!is_error_pending()){
if ((tmp_fileno>0)&&(tmp_fileno<=tqbs->len)){tqbs->chr[tmp_fileno-1]=tmp_long;}else{error(5);}
}}}
if(!qbevent)break;evnt(28345,179,"wiki_methods.bas");}while(r);
error_track_line(28345,181,"wiki_methods.bas");
do{
*__LONG_HELP_POS=*__LONG_HELP_POS+ 1 ;
if(!qbevent)break;evnt(28345,181,"wiki_methods.bas");}while(r);
error_track_line(28345,182,"wiki_methods.bas");
fornext_continue_7860:;
}
fornext_exit_7860:;
exit_subfunc:;
free_mem_lock(sf_mem_lock);
#include "free454.txt"
if ((tmp_mem_static_pointer>=mem_static)&&(tmp_mem_static_pointer<=mem_static_limit)) mem_static_pointer=tmp_mem_static_pointer; else mem_static_pointer=mem_static;
cmem_sp=tmp_cmem_sp;
}
