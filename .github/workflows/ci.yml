name: CI

on:
  push:
    branches:
      - 'main'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  CI-Run:
    uses: ./.github/workflows/build-process.yml
    secrets: inherit
