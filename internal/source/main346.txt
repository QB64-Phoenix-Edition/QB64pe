int32 FUNC_CTRL2(){
qbs *tqbs;
ptrszint tmp_long;
int32 tmp_fileno;
uint32 qbs_tmp_base=qbs_tmp_list_nexti;
uint8 *tmp_mem_static_pointer=mem_static_pointer;
uint32 tmp_cmem_sp=cmem_sp;
#include "data346.txt"
mem_lock *sf_mem_lock;
new_mem_lock();
sf_mem_lock=mem_lock_tmp;
sf_mem_lock->type=3;
if (is_error_pending()) goto exit_subfunc;
error_track_line(25867,18644,"ide_methods.bas");
S_60548:;
if ((*__LONG_MACOSX)||is_error_pending()){
if(qbevent){evnt(25867,18644,"ide_methods.bas");if(r)goto S_60548;}
error_track_line(25867,18645,"ide_methods.bas");
S_60549:;
if ((func__keydown( 100309 ))||is_error_pending()){
if(qbevent){evnt(25867,18645,"ide_methods.bas");if(r)goto S_60549;}
error_track_line(25867,18645,"ide_methods.bas");
do{
*_FUNC_CTRL2_LONG_CTRL2= 1 ;
if(!qbevent)break;evnt(25867,18645,"ide_methods.bas");}while(r);
error_track_line(25867,18645,"ide_methods.bas");
}
error_track_line(25867,18646,"ide_methods.bas");
S_60552:;
if ((func__keydown( 100310 ))||is_error_pending()){
if(qbevent){evnt(25867,18646,"ide_methods.bas");if(r)goto S_60552;}
error_track_line(25867,18646,"ide_methods.bas");
do{
*_FUNC_CTRL2_LONG_CTRL2= 1 ;
if(!qbevent)break;evnt(25867,18646,"ide_methods.bas");}while(r);
error_track_line(25867,18646,"ide_methods.bas");
}
error_track_line(25867,18647,"ide_methods.bas");
}
exit_subfunc:;
free_mem_lock(sf_mem_lock);
#include "free346.txt"
if ((tmp_mem_static_pointer>=mem_static)&&(tmp_mem_static_pointer<=mem_static_limit)) mem_static_pointer=tmp_mem_static_pointer; else mem_static_pointer=mem_static;
cmem_sp=tmp_cmem_sp;
return *_FUNC_CTRL2_LONG_CTRL2;
}
