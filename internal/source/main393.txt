int32 FUNC_HELP_COL(){
qbs *tqbs;
ptrszint tmp_long;
int32 tmp_fileno;
uint32 qbs_tmp_base=qbs_tmp_list_nexti;
uint8 *tmp_mem_static_pointer=mem_static_pointer;
uint32 tmp_cmem_sp=cmem_sp;
#include "data393.txt"
mem_lock *sf_mem_lock;
new_mem_lock();
sf_mem_lock=mem_lock_tmp;
sf_mem_lock->type=3;
if (is_error_pending()) goto exit_subfunc;
do{
*_FUNC_HELP_COL_LONG_COL=*__LONG_HELP_COL_NORMAL;
if(!qbevent)break;evnt(25867,249,"wiki_methods.bas");}while(r);
S_63747:;
if ((*__LONG_HELP_ITALIC)||is_error_pending()){
if(qbevent){evnt(25867,250,"wiki_methods.bas");if(r)goto S_63747;}
do{
*_FUNC_HELP_COL_LONG_COL=*__LONG_HELP_COL_ITALIC;
if(!qbevent)break;evnt(25867,250,"wiki_methods.bas");}while(r);
}
S_63750:;
if ((*__LONG_HELP_BOLD)||is_error_pending()){
if(qbevent){evnt(25867,251,"wiki_methods.bas");if(r)goto S_63750;}
do{
*_FUNC_HELP_COL_LONG_COL=*__LONG_HELP_COL_BOLD;
if(!qbevent)break;evnt(25867,251,"wiki_methods.bas");}while(r);
}
S_63753:;
if ((*__LONG_HELP_HEADING)||is_error_pending()){
if(qbevent){evnt(25867,252,"wiki_methods.bas");if(r)goto S_63753;}
do{
*_FUNC_HELP_COL_LONG_COL=*__LONG_HELP_COL_SECTION;
if(!qbevent)break;evnt(25867,252,"wiki_methods.bas");}while(r);
}
S_63756:;
if ((*__LONG_HELP_LINKTXT)||is_error_pending()){
if(qbevent){evnt(25867,253,"wiki_methods.bas");if(r)goto S_63756;}
S_63757:;
if ((-(*__LONG_HELP_LOCKPARSE== 0 ))||is_error_pending()){
if(qbevent){evnt(25867,255,"wiki_methods.bas");if(r)goto S_63757;}
do{
*_FUNC_HELP_COL_LONG_COL=*__LONG_HELP_COL_LINK;
if(!qbevent)break;evnt(25867,255,"wiki_methods.bas");}while(r);
}else{
do{
*_FUNC_HELP_COL_LONG_COL=*__LONG_HELP_COL_ITALIC;
if(!qbevent)break;evnt(25867,255,"wiki_methods.bas");}while(r);
}
}
do{
*_FUNC_HELP_COL_LONG_HELP_COL=*_FUNC_HELP_COL_LONG_COL;
if(!qbevent)break;evnt(25867,257,"wiki_methods.bas");}while(r);
exit_subfunc:;
free_mem_lock(sf_mem_lock);
#include "free393.txt"
if ((tmp_mem_static_pointer>=mem_static)&&(tmp_mem_static_pointer<=mem_static_limit)) mem_static_pointer=tmp_mem_static_pointer; else mem_static_pointer=mem_static;
cmem_sp=tmp_cmem_sp;
return *_FUNC_HELP_COL_LONG_HELP_COL;
}
