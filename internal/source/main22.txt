void SUB_CLOSEMAIN(){
qbs *tqbs;
ptrszint tmp_long;
int32 tmp_fileno;
uint32 qbs_tmp_base=qbs_tmp_list_nexti;
uint8 *tmp_mem_static_pointer=mem_static_pointer;
uint32 tmp_cmem_sp=cmem_sp;
#include "data22.txt"
mem_lock *sf_mem_lock;
new_mem_lock();
sf_mem_lock=mem_lock_tmp;
sf_mem_lock->type=3;
if (is_error_pending()) goto exit_subfunc;
S_17606:;
if ((-(FUNC_GETRCSTATEVAR((void*)( ((char*)(__UDT_VWATCHON)) + ((0+0)) ))== 1 ))||is_error_pending()){
if(qbevent){evnt(15177);if(r)goto S_17606;}
do{
SUB_VWATCHADDLABEL(&(pass2596= 0 ),&(pass2597= -1 ));
if(!qbevent)break;evnt(15177);}while(r);
}
do{
SUB_XEND();
if(!qbevent)break;evnt(15178);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2598=*__LONG_MAINTXTBUF),qbs_new_txt_len("return;",7));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15179);}while(r);
S_17611:;
if ((-(FUNC_GETRCSTATEVAR((void*)( ((char*)(__UDT_VWATCHON)) + ((0+0)) ))== 1 ))||is_error_pending()){
if(qbevent){evnt(15180);if(r)goto S_17611;}
do{
SUB_WRITEBUFLINE(&(pass2599=*__LONG_MAINTXTBUF),qbs_new_txt_len("VWATCH_SETNEXTLINE:;",20));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15181);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2600=*__LONG_MAINTXTBUF),qbs_new_txt_len("switch (*__LONG_VWATCH_GOTO) {",30));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15182);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2601=*__LONG_MAINTXTBUF),qbs_add(qbs_add(qbs_add(qbs_new_txt_len("#include ",9),func_chr( 34 )),qbs_new_txt_len("vw_main_dispatch.txt",20)),func_chr( 34 )));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15183);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2602=*__LONG_MAINTXTBUF),qbs_new_txt_len("    default:",12));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15184);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2603=*__LONG_MAINTXTBUF),qbs_new_txt_len("        *__LONG_VWATCH_GOTO=*__LONG_VWATCH_LINENUMBER;",54));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15185);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2604=*__LONG_MAINTXTBUF),qbs_new_txt_len("        goto VWATCH_SETNEXTLINE;",32));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15186);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2605=*__LONG_MAINTXTBUF),qbs_new_txt_len("}",1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15187);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2606=*__LONG_MAINTXTBUF),qbs_new_txt_len("VWATCH_SKIPLINE:;",17));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15189);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2607=*__LONG_MAINTXTBUF),qbs_new_txt_len("switch (*__LONG_VWATCH_GOTO) {",30));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15190);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2608=*__LONG_MAINTXTBUF),qbs_add(qbs_add(qbs_add(qbs_new_txt_len("#include ",9),func_chr( 34 )),qbs_new_txt_len("vw_main_skip.txt",16)),func_chr( 34 )));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15191);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2609=*__LONG_MAINTXTBUF),qbs_new_txt_len("}",1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15192);}while(r);
}
do{
SUB_WRITEBUFLINE(&(pass2610=*__LONG_MAINTXTBUF),qbs_new_txt_len("}",1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15195);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2611=*__LONG_RETTXTBUF),qbs_new_txt_len("}",1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15196);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2612=*__LONG_RETTXTBUF),qbs_new_txt_len("}",1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15197);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2613=*__LONG_RETTXTBUF),qbs_new_txt_len("error(3);",9));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15198);}while(r);
do{
*_SUB_CLOSEMAIN_LONG_MAININCBUF=FUNC_OPENBUFFER(qbs_new_txt_len("O",1),qbs_add(__STRING_TMPDIR,qbs_new_txt_len("main.txt",8)));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15200);}while(r);
S_17629:;
fornext_value2615= 0 ;
fornext_finalvalue2615=*__LONG_SUBFUNCNLAST;
fornext_step2615= 1 ;
if (fornext_step2615<0) fornext_step_negative2615=1; else fornext_step_negative2615=0;
if (is_error_pending()) goto fornext_error2615;
goto fornext_entrylabel2615;
while(1){
fornext_value2615=fornext_step2615+(*_SUB_CLOSEMAIN_LONG_I);
fornext_entrylabel2615:
*_SUB_CLOSEMAIN_LONG_I=fornext_value2615;
if (fornext_step_negative2615){
if (fornext_value2615<fornext_finalvalue2615) break;
}else{
if (fornext_value2615>fornext_finalvalue2615) break;
}
fornext_error2615:;
if(qbevent){evnt(15201);if(r)goto S_17629;}
do{
SUB_WRITEBUFLINE(&(pass2616=*_SUB_CLOSEMAIN_LONG_MAININCBUF),qbs_add(qbs_add(qbs_add(qbs_add(qbs_add(qbs_new_txt_len("#include ",9),func_chr( 34 )),qbs_new_txt_len("main",4)),qbs__tostr((int32)(*_SUB_CLOSEMAIN_LONG_I),NULL,0)),qbs_new_txt_len(".txt",4)),func_chr( 34 )));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15202);}while(r);
fornext_continue_2614:;
}
fornext_exit_2614:;
do{
SUB_WRITEBUFLINE(&(pass2617=*_SUB_CLOSEMAIN_LONG_MAININCBUF),qbs_new_txt_len("qbs *func__compdate() {",23));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15204);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2618=*_SUB_CLOSEMAIN_LONG_MAININCBUF),qbs_new_txt_len("return qbs_new_txt(__DATE__);",29));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15205);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2619=*_SUB_CLOSEMAIN_LONG_MAININCBUF),qbs_new_txt_len("}",1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15206);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2620=*_SUB_CLOSEMAIN_LONG_MAININCBUF),qbs_new_txt_len("qbs *func__comptime() {",23));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15207);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2621=*_SUB_CLOSEMAIN_LONG_MAININCBUF),qbs_new_txt_len("return qbs_new_txt(__TIME__);",29));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15208);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2622=*_SUB_CLOSEMAIN_LONG_MAININCBUF),qbs_new_txt_len("}",1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15209);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2623=*_SUB_CLOSEMAIN_LONG_MAININCBUF),qbs_new_txt_len("qbs *func__compvers() {",23));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15210);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2624=*_SUB_CLOSEMAIN_LONG_MAININCBUF),qbs_add(qbs_add(qbs_add(qbs_add(qbs_add(qbs_new_txt_len("return qbs_new_txt(",19),func_chr( 34 )),qbs_new_txt_len("QB64-PE v",9)),__STRING_VERSION),func_chr( 34 )),qbs_new_txt_len(");",2)));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15211);}while(r);
do{
SUB_WRITEBUFLINE(&(pass2625=*_SUB_CLOSEMAIN_LONG_MAININCBUF),qbs_new_txt_len("}",1));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15212);}while(r);
do{
*__LONG_FIRSTLINENUMBERLABELVWATCH= 0 ;
if(!qbevent)break;evnt(15214);}while(r);
exit_subfunc:;
free_mem_lock(sf_mem_lock);
#include "free22.txt"
if ((tmp_mem_static_pointer>=mem_static)&&(tmp_mem_static_pointer<=mem_static_limit)) mem_static_pointer=tmp_mem_static_pointer; else mem_static_pointer=mem_static;
cmem_sp=tmp_cmem_sp;
}
