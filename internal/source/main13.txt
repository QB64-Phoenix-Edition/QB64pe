void SUB_UDTAPPLYFIELDMODE(qbs*_SUB_UDTAPPLYFIELDMODE_STRING_TOKEN,int32*_SUB_UDTAPPLYFIELDMODE_LONG_FIELDMODE){
qbs *tqbs;
ptrszint tmp_long;
int32 tmp_fileno;
uint32 qbs_tmp_base=qbs_tmp_list_nexti;
uint8 *tmp_mem_static_pointer=mem_static_pointer;
uint32 tmp_cmem_sp=cmem_sp;
#include "data13.txt"
mem_lock *sf_mem_lock;
new_mem_lock();
sf_mem_lock=mem_lock_tmp;
sf_mem_lock->type=3;
libqb_check_stack();
if (is_error_pending()) goto exit_subfunc;
do{
if(!qbevent)break;evnt(15615);}while(r);
do{
*_SUB_UDTAPPLYFIELDMODE_LONG_PARSED_MODE=FUNC_UDTFIELDMODETOKEN(_SUB_UDTAPPLYFIELDMODE_STRING_TOKEN);
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15617);}while(r);
S_18140:;
if ((-(*_SUB_UDTAPPLYFIELDMODE_LONG_PARSED_MODE== 0 ))||is_error_pending()){
if(qbevent){evnt(15618);if(r)goto S_18140;}
do{
goto exit_subfunc;
if(!qbevent)break;evnt(15618);}while(r);
}
S_18143:;
if ((-(((int8*)(__ARRAY_BYTE_UNSTABLEFLAGS[0]))[array_check((*__LONG_UNSTABLE_TYPEFIELDS)-__ARRAY_BYTE_UNSTABLEFLAGS[4],__ARRAY_BYTE_UNSTABLEFLAGS[5])]== 0 ))||is_error_pending()){
if(qbevent){evnt(15619);if(r)goto S_18143;}
do{
SUB_GIVE_ERROR(qbs_new_txt_len("_Static/_Dynamic require $Unstable:TypeFields",45));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15620);}while(r);
do{
goto exit_subfunc;
if(!qbevent)break;evnt(15621);}while(r);
}
S_18147:;
if ((-(*_SUB_UDTAPPLYFIELDMODE_LONG_FIELDMODE!= 0 ))||is_error_pending()){
if(qbevent){evnt(15623);if(r)goto S_18147;}
do{
SUB_GIVE_ERROR(qbs_new_txt_len("A TYPE member array cannot use both _Dynamic and _Static",56));
qbs_cleanup(qbs_tmp_base,0);
if(!qbevent)break;evnt(15624);}while(r);
do{
goto exit_subfunc;
if(!qbevent)break;evnt(15625);}while(r);
}
do{
*_SUB_UDTAPPLYFIELDMODE_LONG_FIELDMODE=*_SUB_UDTAPPLYFIELDMODE_LONG_PARSED_MODE;
if(!qbevent)break;evnt(15627);}while(r);
exit_subfunc:;
free_mem_lock(sf_mem_lock);
#include "free13.txt"
if ((tmp_mem_static_pointer>=mem_static)&&(tmp_mem_static_pointer<=mem_static_limit)) mem_static_pointer=tmp_mem_static_pointer; else mem_static_pointer=mem_static;
cmem_sp=tmp_cmem_sp;
}
