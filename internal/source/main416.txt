void SUB_CLEARMOUSE(){
qbs *tqbs;
ptrszint tmp_long;
int32 tmp_fileno;
uint32 qbs_tmp_base=qbs_tmp_list_nexti;
uint8 *tmp_mem_static_pointer=mem_static_pointer;
uint32 tmp_cmem_sp=cmem_sp;
#include "data416.txt"
mem_lock *sf_mem_lock;
new_mem_lock();
sf_mem_lock=mem_lock_tmp;
sf_mem_lock->type=3;
libqb_check_stack();
if (is_error_pending()) goto exit_subfunc;
do{
*__LONG_ICHANGED= 0 ;
if(!qbevent)break;evnt(28196,18925,"ide_methods.bas");}while(r);
do{
*__LONG_MB= 0 ;
if(!qbevent)break;evnt(28196,18926,"ide_methods.bas");}while(r);
do{
*__LONG_MB2= 0 ;
if(!qbevent)break;evnt(28196,18927,"ide_methods.bas");}while(r);
do{
*__LONG_MCLICK= 0 ;
if(!qbevent)break;evnt(28196,18928,"ide_methods.bas");}while(r);
do{
*__LONG_MRELEASE= 0 ;
if(!qbevent)break;evnt(28196,18929,"ide_methods.bas");}while(r);
S_65066:;
while((func__mousebutton( 1 )|func__mousebutton( 2 ))||is_error_pending()){
if(qbevent){evnt(28196,18930,"ide_methods.bas");if(r)goto S_65066;}
do{
*_SUB_CLEARMOUSE_LONG_I=func__mouseinput();
if(!qbevent)break;evnt(28196,18931,"ide_methods.bas");}while(r);
dl_continue_6590:;
}
dl_exit_6590:;
exit_subfunc:;
free_mem_lock(sf_mem_lock);
#include "free416.txt"
if ((tmp_mem_static_pointer>=mem_static)&&(tmp_mem_static_pointer<=mem_static_limit)) mem_static_pointer=tmp_mem_static_pointer; else mem_static_pointer=mem_static;
cmem_sp=tmp_cmem_sp;
}
